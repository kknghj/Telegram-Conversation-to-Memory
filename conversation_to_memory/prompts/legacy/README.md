# Legacy Prompts

These prompts are retained as historical references only.

- `interviewer_system_prompt.txt`: deprecated multi-question interview flow.
- `memory_extraction_prompt.txt`: deprecated extraction prompt replaced by `../memory_archive_system_prompt.txt`.

Runtime prompt loading should use the non-legacy prompt files in `conversation_to_memory/prompts/`.
